# 进度日志

## 2026-04-27
- 初始化任务规划文件。
- 已完成代码基线审阅（首页、库存、订单列表、运单详情、新增运单、DAO）。
- 完成 6 项需求落地：
  - 首页统计改为 今日/昨日/未完成。
  - 库位信息透出到库存明细与订单链路。
  - 库存明细新增产品编号快捷筛选。
  - 订单信息默认今日，并新增 今日/昨日/一周/一月/未完成 快捷筛选。
  - 运单详情新增编辑订单与删除订单。
  - 新增运单改为“继续添加/完成”，支持同运单连续追加产品并在完成后清空。
- 已通过验证：
  - `flutter analyze`
  - `flutter test`
  - `flutter build apk --debug`

## 2026-04-28
- 根据 `1.xlsx`（第4个sheet）重建内置库存种子数据：
  - 库位按 `AR/AS` 读取，空白不写入。
  - 一板箱数按 `AT` 读取。
  - 仅写入库存箱数（AN）大于 0 的行。
  - 总件数校验为 `3,155,730`。
- 更新种子导入逻辑：不再写死“浙江仓”，改为读取种子内 `location`。
- APP 图标替换为根目录 `jiemei.png`，并重新生成 Android launcher 图标资源。
- 验证通过：
  - `flutter analyze`
  - `flutter test`
  - `flutter build apk --debug`

- 库存明细交互继续优化：
  - 默认分组折叠展示。
  - 产品分组和产品筛选标签改为按库存数量降序（非产品编号排序）。
  - 内置数据库备注改为读取 `AO` 列（多数为空时保持空）。
